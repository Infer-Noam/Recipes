import { type GridSize } from "@mui/material";
import type { ResponsiveStyleValue } from "@mui/system";

const gridItemSize: ResponsiveStyleValue<GridSize> = { xs: 6, lg: 4.5, xl: 6 };

export default {
  gridItemSize,
};
