import { type GridSize } from "@mui/material";
import type { ResponsiveStyleValue } from "@mui/system";

const gridItemSize: ResponsiveStyleValue<GridSize> = { xs: 12, lg: 8, xl: 6 };

export default {
  gridItemSize,
};
