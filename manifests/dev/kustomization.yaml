apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - server-configmap.dev.yaml
  - server-deployment.dev.yaml
  - ingress.dev.yaml

namespace: team-tikchalal

labels:
  - pairs:
      env: dev
      project: recipes